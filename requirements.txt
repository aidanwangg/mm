Flask>=3.0
numpy>=1.24
scikit-learn>=1.3
requests>=2.28
# Optional: enables the UMAP projection. The app degrades gracefully without it
# (UMAP button will report it's missing; PCA / t-SNE / Direct still work).
umap-learn>=0.5
